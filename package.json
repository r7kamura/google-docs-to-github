{
  "private": true,
  "scripts": {
    "build": "npx ncc build index.js --license licenses.txt",
    "lint": "npx prettier --write ."
  },
  "dependencies": {
    "@actions/core": "^1.7.0",
    "googleapis": "^68.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.33.4",
    "prettier": "2.6.2"
  }
}
